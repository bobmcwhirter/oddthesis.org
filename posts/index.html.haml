---
title: Posts
layout: sidebar
---

#main-column
  = page.posts.links

  - for post in page.posts
    .post
      .title
        %a{:href=>post.url}= post.title
      .details
        .author
          = post.author
        .date
          #{post.date.strftime( '%d %B %Y' )}
      .content
        = post.content

  = page.posts.links
#sidebar
